<div id="shareModal" class="share-modal" style="display: none;">
    <div class="share-modal-content">
        <div class="share-modal-header">
            <h3>Chia sẻ bài viết</h3>
            <span class="share-modal-close">&times;</span>
        </div>
        <div class="share-modal-body">
            <p class="share-hint">Chọn người bạn muốn gửi bài viết này:</p>

            <div id="chatUserList" class="share-user-list">
                <div style="text-align: center; color: #999;">Đang tải danh sách...</div>
            </div>
        </div>
    </div>
</div>

<style>
    .share-modal {
        display: none; /* Mặc định ẩn */
        position: fixed;
        z-index: 9999; /* Đảm bảo nằm trên cùng */
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        overflow: auto;
        background-color: rgba(0,0,0,0.5); /* Màu nền đen mờ */
        animation: fadeIn 0.3s;
    }

    .share-modal-content {
        background-color: #fefefe;
        margin: 10% auto; /* Cách trên 10% và giữa màn hình */
        padding: 0;
        border: 1px solid #888;
        width: 400px; /* Độ rộng cố định */
        max-width: 90%;
        border-radius: 12px;
        box-shadow: 0 4px 20px rgba(0,0,0,0.2);
        animation: slideIn 0.3s;
    }

    .share-modal-header {
        padding: 15px 20px;
        border-bottom: 1px solid #eee;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #fff;
        border-radius: 12px 12px 0 0;
    }

    .share-modal-header h3 {
        margin: 0;
        font-size: 18px;
        font-weight: 600;
    }

    .share-modal-close {
        color: #aaa;
        font-size: 28px;
        font-weight: bold;
        cursor: pointer;
        line-height: 1;
    }

    .share-modal-close:hover,
    .share-modal-close:focus {
        color: black;
        text-decoration: none;
    }

    .share-modal-body {
        padding: 20px;
        max-height: 400px;
        overflow-y: auto;
    }

    .share-hint {
        margin-bottom: 15px;
        color: #65676b;
        font-size: 14px;
    }

    /* CSS cho danh sách user trong modal */
    .chat-user-item {
        display: flex;
        align-items: center;
        padding: 10px;
        border-radius: 8px;
        cursor: pointer;
        transition: background 0.2s;
        border-bottom: 1px solid #f0f2f5;
    }

    .chat-user-item:hover {
        background-color: #f0f2f5;
    }

    .chat-user-item img {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        margin-right: 12px;
        object-fit: cover;
    }

    .chat-user-info {
        flex-grow: 1;
    }

    .chat-user-name {
        font-weight: 600;
        font-size: 15px;
        color: #050505;
    }

    .chat-user-status {
        font-size: 12px;
        color: #4CAF50;
    }

    @@keyframes fadeIn {
        from {opacity: 0}
        to {opacity: 1}
    }

    @@keyframes slideIn {
        from {transform: translateY(-50px); opacity: 0;}
        to {transform: translateY(0); opacity: 1;}
    }
</style>